#include <stdio.h>

int main(void) {
    const char *name = "YourName"; /* replace YourName with your name */
    int i = 0;
    while (i < 10) {
        printf("%s\n", name);
        i++;
    }
    return 0;
}
